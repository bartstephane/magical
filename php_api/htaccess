RewriteEngine On
RewriteBase /

# Désactiver MultiViews qui peut interférer
Options -MultiViews

# /api → api.php
RewriteRule ^api/?$ api.php [QSA,L]

# /api/... → api.php (en utilisant un paramètre GET "route")
RewriteRule ^api/(.*)$ api.php?route=$1 [QSA,L]
